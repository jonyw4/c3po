# Family knowledge base

Purpose: store **durable** information (routines, decisions, contacts) in Markdown for WhatsApp lookup.

Rules:
- Do not store secrets (passwords, tokens, documents, full ID numbers).
- Prefer stable facts and decisions ("we decided X") over entire conversations.
