# C3PO Environment Variables
# Copy to ~/.config/c3po/.env and fill in real values.
# The setup script (deploy/exe-dev-setup.sh) generates this automatically.

# Anthropic API key for the LLM backend
ANTHROPIC_API_KEY=sk-ant-CHANGE-ME

# Gateway authentication password (protects against unauthorized access).
# Generate with: openssl rand -base64 32
GATEWAY_PASSWORD=

# Gateway port (default: 18789, localhost only)
GATEWAY_PORT=18789

# Mercado Livre API — necessário para a skill de shopping (c3po-shopping-ml.ts)
# Obtenha em: https://developers.mercadolibre.com.br/
ML_APP_ID=
ML_APP_SECRET=

# Opção 1 (preferida): access_token direto — dura ~6h, renovar via OAuth quando expirar.
# Gerar: curl -X POST https://api.mercadolibre.com/oauth/token \
#   -d "grant_type=authorization_code&client_id=$ML_APP_ID&client_secret=$ML_APP_SECRET \
#       &code=TG-xxx&redirect_uri=https://SUA_URI"
ML_ACCESS_TOKEN=

# Opção 2: refresh_token para renovação automática (se disponível no fluxo OAuth).
ML_REFRESH_TOKEN=
